#include "main.h"
#include <string.h>
/**
* _strlen -  returns the length of a string.
*@s: pointer to string
* Return: the length of string
*/
int _strlen(char *s)
{
	 s = "My first strlen!";

	return (strlen(s));



}
